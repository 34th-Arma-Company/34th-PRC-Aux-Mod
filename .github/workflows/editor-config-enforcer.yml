name: "Editor Config Enforcer"
on:
- pull_request

jobs:
  labeler:
    permissions:
      contents: read
    #  pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: zbeekman/EditorConfig-Action@v1.1.1
