name: "Editor Config Enforcer"
on:
- pull_request

jobs:
  editer-config-enforcer:
    permissions:
      contents: read
    #  pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: wow-rp-addons/actions-editorconfig-check@v1.0.1
    # greut/eclint-action@v0
    # editorconfig-checker/action-editorconfig-checker@v1.0.0
    # snow-actions/eclint@v1.0.1
